# Upstream proprietary blob — large (~200 MB) and licensed.
AmpLockerLinux.zip

# Flatpak-builder outputs and caches
.build/
.flatpak-builder/
builddir/
repo/

# Justfile working dir (unpacked upstream tree)
amplocker/

# Editor / OS cruft
*.swp
*.swo
.DS_Store
__MACOSX/
.idea/
.vscode/

# AI agent / assistant local state
# (committed instruction files like AGENTS.md, CLAUDE.md, .cursorrules
#  are intentionally NOT ignored — they belong in version control.)
.claude/
.cursor/
.opencode/
.continue/
.codeium/
.tabnine/
.github/copilot/
.aider*

